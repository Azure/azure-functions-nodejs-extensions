{
    "name": "serviceBusBinaryDataAccess",
    "version": "1.0.0",
    "description": "Sample demonstrating raw binary data access with @azure/functions-extensions-servicebus v0.4.0",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "clean": "rimraf dist",
        "prestart": "npm run clean && npm run build",
        "start": "func start",
        "test": "echo \"No tests yet...\""
    },
    "dependencies": {
        "@azure/functions": "^4.11.0",
        "@azure/functions-extensions-servicebus": "0.4.0",
        "@azure/identity": "^4.13.0",
        "@azure/service-bus": "^7.9.5"
    },
    "devDependencies": {
        "@types/node": "^20.x",
        "rimraf": "^5.0.0",
        "typescript": "^4.0.0"
    },
    "main": "dist/src/{index.js,functions/*.js}"
}
