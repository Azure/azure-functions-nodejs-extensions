{
    "name": "serviceBusTriggerExponentialBackOff",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "clean": "rimraf dist",
        "prestart": "npm run clean && npm run build",
        "start": "func start",
        "test": "echo \"No tests yet...\""
    },
    "dependencies": {
        "@azure/functions": "4.7.4-preview",
        "@azure/functions-extensions-servicebus": "0.3.5-preview",
        "@azure/identity": "^4.13.0"
    },
    "devDependencies": {
        "@types/node": "^20.x",
        "rimraf": "^5.0.0",
        "typescript": "^4.0.0",
        "undici": "^5.13.0"
    },
    "main": "dist/src/{index.js,functions/*.js}"
}
